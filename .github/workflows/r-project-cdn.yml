name: R Project CDN Publish

on:
  schedule:
    # 23:07 KST. Runs after notebook generation and before the next package window.
    - cron: "7 14 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  cdn:
    name: CDN publish and admin snapshot
    uses: ./.github/workflows/r-project-all.yml
    with:
      scope: cdn
    secrets: inherit
